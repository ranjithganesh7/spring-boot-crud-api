# Spring-Boot-CRUD-api
A RESTful CRUD API built with Spring Boot, Spring Data JPA, and MySQL for managing user data. This project demonstrates the implementation of Create, Read, Update, and Delete operations using a layered architecture. APIs were tested using Postman, and the application was developed in IntelliJ IDEA.
